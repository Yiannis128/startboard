{
  "name": "startboard",
  "version": "1.0.0",
  "description": "Start page for Chrome.",
  "main": "tailwind.config.js",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o src/output.css",
    "watch": "tailwindcss -i src/input.css -o src/output.css --watch",
    "build": "npm run build:css && mkdir -p dist && zip -r dist/startboard.zip manifest.json src LICENSE",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "daisyui": "^5.5.14",
    "tailwindcss": "^4.1.18"
  }
}
