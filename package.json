{
  "name": "startboard",
  "version": "0.1.1",
  "description": "Open source start page, published as a Chrome extension and a PWA.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o src/output.css",
    "watch": "tailwindcss -i src/input.css -o src/output.css --watch",
    "build:extension": "node scripts/build-extension.js",
    "build:pwa": "node scripts/build-pwa.js",
    "build:all": "node scripts/build-extension.js && node scripts/build-pwa.js",
    "test": "node scripts/build-pwa.js && bun test",
    "test:only": "bun test"
  },
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "daisyui": "^5.5.14",
    "jsdom": "^30.0.1",
    "tailwindcss": "^4.1.18"
  }
}
